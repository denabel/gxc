% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gxc_link_dwd.R
\name{gxc_link_dwd}
\alias{gxc_link_dwd}
\title{Link DWD Weather Data to Geospatial Data with Specific Observation Dates}
\usage{
gxc_link_dwd(
  data,
  date_variable,
  what = c("air_temperature_mean", "air_temperature_max", "precipitation"),
  days_back = NULL,
  months = NULL,
  years_back = NULL,
  reference_years = NULL,
  buffers = NULL,
  reference_statistic = function(x) {
     mean(x, na.rm = TRUE)
 },
  crs = 3035,
  where_processed = NULL,
  where_raw = NULL,
  where_secret = NULL,
  cache = TRUE
)
}
\arguments{
\item{data}{A spatial dataset (sf object) containing geometries and
attributes.}

\item{date_variable}{A character string specifying the date-related variable
in the dataset to use for linking the weather data.}

\item{what}{A character vector specifying the weather variables to extract
(default: air temperature mean, air temperature max, and precipitation).}

\item{days_back}{Optional integer specifying how many days back to consider
for weather data.}

\item{months}{Optional vector specifying the months to consider.}

\item{years_back}{Optional integer specifying how many years back to consider
for weather data.}

\item{reference_years}{Optional vector specifying the reference years to
consider for calculating statistics.}

\item{buffers}{A numeric vector specifying buffer distances for spatial
extraction.}

\item{reference_statistic}{A function to compute the reference statistic
(default: mean with \code{na.rm = TRUE}).}

\item{crs}{The coordinate reference system for the output
(default: EPSG 3035).}

\item{where_processed}{Optional directory path for storing processed data.}

\item{where_raw}{Optional directory path for storing raw data.}

\item{where_secret}{Optional directory path for secret data storage (created
if it does not exist).}

\item{cache}{Logical; whether to cache results (default: TRUE).}
}
\value{
A data frame or tibble with the linked weather data.
}
\description{
This function links geospatial data including specific observation dates with
weather data from the German Weather Service (DWD).
}
\details{
This function splits the input data by the specified date variable and links
it to weather data from the DWD for each group.
It handles spatial data with different geometry types (e.g., POINT geometries
with buffer adjustments) and supports a caching mechanism for efficient
processing.
}
\examples{
\dontrun{
result <- 
  gxc_link_dwd(
    data = my_sf_data, 
    date_variable = "date_column", 
    what = "air_temperature_mean", 
    days_back = 5
  )
}

}
